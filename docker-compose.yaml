version: '3.4'
services:
  r_base:
    image: productor
    restart: always
    environment:
      R_TEST: testing
    volumes:
      - ./main.R:/home/productor/main.R
    ports:
      - 5439:5432
    command: ["Rscript", "/home/productor/main.R"]
